# Intentionally empty: the fetchers, the database, the metric maths, both
# rankers and the LLM clients are pure standard-library Python, which is what
# lets the daily workflow run with no install step at all. Only the tests need
# anything (see requirements-dev.txt).
